package com.company.CarBase;

public class Passenger extends Transport{
    public Passenger(String number, String model, String color, String type) {
        super(number, model, color, type);
    }
}
